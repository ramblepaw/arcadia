services:
  game-hub:
    build: .
    ports:
      - "8090:8090"
    volumes:
      - ./data:/data
    environment:
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=change-me-before-deploying
      - TRUST_PROXY=false
    restart: unless-stopped
